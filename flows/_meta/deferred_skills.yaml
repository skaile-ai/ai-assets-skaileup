# flows/_meta/deferred_skills.yaml
#
# Skills referenced by SKILL_GRAPH § 6 tier-composition table (and therefore
# by Phase 2 tier flows) that have no Phase-2 mini-plan authoring them.
# The verifier (flows/_meta/verify_flows.py) emits WARNING (not ERROR) for
# these names. Phase 3 must close this list.
#
# Each entry MUST carry a one-line rationale per the Task 2H executing-agent
# decisions (see plan § "Open Questions" #1 and #2).
deferred_phase_3:
  - name: concept-goals
    reason: "Phase 3 — concept-goals (referenced by standard-app/complex-app)"
  - name: concept-comparable
    reason: "Phase 3 — concept-comparable (referenced by standard-app/complex-app)"
  - name: design-inspiration
    reason: "Phase 3 — design-inspiration (referenced by standard-app/complex-app)"
  - name: walkthrough-mockup-lit
    reason: "Phase 3 — walkthrough-mockup-lit (alt for embedded contexts in complex-app)"
  - name: walkthrough-mockup-astro
    reason: "Phase 3 — walkthrough-mockup-astro (default mockup for standard-app)"
  - name: walkthrough-mockup-framework
    reason: "Phase 3 — walkthrough-mockup-framework (complex-app)"
  - name: mockup-feedback-annotate
    reason: "Phase 3 — mockup-feedback-annotate (mockup feedback cluster, standard/complex)"
  - name: mockup-feedback-triage
    reason: "Phase 3 — mockup-feedback-triage (mockup feedback cluster, standard/complex)"
  - name: mockup-feedback-patch
    reason: "Phase 3 — mockup-feedback-patch (mockup feedback cluster, standard/complex)"
  - name: mockup-feedback-apply
    reason: "Phase 3 — mockup-feedback-apply (mockup feedback cluster, standard/complex)"
  - name: impl-architecture-templates
    reason: "ambiguous — Phase 1 promoted templates as stack-specific (template-postxl); needs SKILL_GRAPH § 6 reconciliation"
