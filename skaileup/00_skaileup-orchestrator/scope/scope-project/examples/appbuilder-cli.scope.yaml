schema_version: "1.0"
shape: cli
tier: appbuilder-cli
flow_to_run: "flow:appbuilder-cli"
reasoning: |
  Stage 0 shape check fires: this is a headless command-line tool with no UI,
  so it routes to appbuilder-cli and the sizing rule is skipped. The recorded signals
  (3 features, single-user, structured config persistence, no integrations)
  inform concept depth but do not change the route — any-size CLI uses appbuilder-cli.
description: "A CLI that converts CSV files to JSON with column filtering and schema validation."
signals:
  features_estimate: 3
  multi_user: false
  persistence: structured
  integrations: []
override:
  applied: false
  requested_tier: null
  rule_would_have_picked: null
chosen_at: "2026-05-07T00:00:00Z"
chosen_by: "skaileup-scope-scope-project@1.2.0"
