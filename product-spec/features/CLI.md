# features CLI

## Trigger

Invoke with: "define features", "what should the app do", "plan functionality", or after journeys are approved.

## Output

- `_concept/experience/features/<NN_group>/<feature>.md` (one file per feature)

## Next Steps

After human approval:
- `screens` — specify UI screens for these features
- `datamodel` — design the data schema
- `concept-orchestrator` — continue the full pipeline
